services:
  frontend:
    build:
      context:
      dockerfile: Dockerfile
    container_name: frontend
    restart: always
    ports:
      - "5173:5173"
    depends_on:
      - postgres
    environment:
      - VITE_HOST=0.0.0.0
